# Final Tap Wins Website

A minimal, responsive landing page for **Final Tap Wins**.

Built with plain HTML, CSS, and JavaScript. No frameworks, no build tools, no external dependencies.

## Quick Start

Open `index.html` directly in your browser, or serve it locally:

```bash
# Python 3
python -m http.server 8000

# Then visit http://localhost:8000
```

## File Structure

```
final-tap-wins-website/
├── index.html          # Single-page site (currently Coming Soon)
├── css/
│   └── style.css       # Design system — colors, typography, layout, responsive
├── js/
│   └── main.js         # Smooth scroll, mobile nav, scroll animations
├── assets/             # Place images and assets here
├── Dockerfile
└── README.md
```

## Status

Currently showing a **Coming Soon** page. Sections for features, how it works, and pricing are stubbed out as HTML comments in `index.html` — uncomment and populate when ready.
